apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -staging

replicas:
  - name: cloudops-app
    count: 2

configMapGenerator:
  - name: cloudops-config
    behavior: merge

    literals:
      - ENVIRONMENT=staging
      - LOG_LEVEL=info
