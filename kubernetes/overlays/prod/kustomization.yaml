apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

replicas:
  - name: cloudops-app
    count: 3

configMapGenerator:
  - name: cloudops-config
    behavior: merge

    literals:
      - ENVIRONMENT=production
      - LOG_LEVEL=warn
